{
  "name": "youtube-dl-websocket",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "cd api && npm install",
    "start": "run-p dev:api dev:server",
    "dev:api": "npm run start --prefix api",
    "dev:server": "npx snowpack dev --port 8081",
    "prod:static": "node index.js",
    "prod:api": "npm run prod --prefix api",
    "build:azure": "npx snowpack build",
    "prod": "run-p prod:api prod:static"
  },
  "devDependencies": {
    "http2-proxy": "^5.0.53",
    "npm-run-all": "^4.1.5",
    "snowpack": "^3.8.8"
  },
  "dependencies": {
    "finalhandler": "^1.2.0"
  }
}
